apiVersion: v1
kind: Service
metadata:
  name: {{ include "ble-exporter.fullname" . }}
  labels:
    {{- include "ble-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http-metrics
      protocol: TCP
      name: http-metrics
  selector:
    {{- include "ble-exporter.selectorLabels" . | nindent 4 }}